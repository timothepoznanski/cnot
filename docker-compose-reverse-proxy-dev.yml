services:
  webserver-dev:
    networks:
      - npm-cnot-webserver-net
      - internal-net

  dbserver-dev:
    networks:
      - internal-net

  pma-dev:
    networks:
      - npm-cnot-pma-net
      - internal-net

networks:
  npm-cnot-webserver-net:
    external: true
  npm-cnot-pma-net:
    external: true
  internal-net:
    driver: bridge
